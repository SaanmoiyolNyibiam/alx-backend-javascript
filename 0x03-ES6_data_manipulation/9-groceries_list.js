export default function groceriesList() {
  // create new map from an array of arrays
  const newMap = new Map([['Apples', 10], ['Tomatoes', 10], ['Pasta', 1], ['Rice', 1], ['Banana', 5]]);

  // return new map created
  return newMap;
}
